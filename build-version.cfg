# Config files to store wine/staging version and release number.
# Read from build_wine.sh
WINE_VERSION="10.11"
STAGING_VERSION=""
RELEASE_VERSION="1"
STAGING_ARGS="--all --no-autoconf -W winedevice-Default_Drivers"